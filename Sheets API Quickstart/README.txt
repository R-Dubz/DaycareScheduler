Open a command prompt to this folder (SHIFT+RightClick)
Run npm install to make sure your folder has everything it needs.
Run node quickstart.js
Browse to the provided URL in your web browser.
Click the Accept button.
Copy the code you're given, paste it into the command-line prompt, and press Enter.

If the above steps don't work, do this and try again:
follow step 1 from this page..... up to part e: https://developers.google.com/sheets/quickstart/nodejs